global:
  resolve_timeout: 5m

route:
  receiver: chat-webhook
  group_by: ["alertname"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 3h

receivers:
  - name: chat-webhook
    webhook_configs:
      - url: http://webhook-bridge:5002/alert
        send_resolved: true
